"""Namespace package for pure financial-math helper functions.

Modules here (currently compound_interest.py) contain formulas with no
database, HTTP, or Celery dependencies, so they can be unit tested in
isolation and reused by any service or worker that needs the same math.
"""
