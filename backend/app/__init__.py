"""Root package for the Personal Finance Dashboard backend.

This file intentionally contains no code; its presence marks `app` as a
regular Python package so that absolute imports like `from app.config import
get_settings` resolve correctly, both when running under Uvicorn and under
Alembic/pytest.
"""
